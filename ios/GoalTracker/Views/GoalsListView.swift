import SwiftUI

// GoalsListView is no longer used as a separate screen.
// Goals are shown in the sidebar (ContentView.SidebarView).
// This file remains for compatibility with the Xcode project.

struct GoalsListView: View {
    var body: some View {
        WelcomeDetailView()
    }
}
