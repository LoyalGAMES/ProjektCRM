import SwiftUI

// DashboardView is no longer used as a separate screen.
// The sidebar in ContentView replaces the dashboard.
// This file remains for compatibility with the Xcode project.

struct DashboardView: View {
    var body: some View {
        WelcomeDetailView()
    }
}
